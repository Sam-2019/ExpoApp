import { StyleSheet } from "react-native";

export const textStyle = StyleSheet.create({
  loadingText: {
    fontSize: 30,
    textAlign: 'center'
  },
  header: {
    // textAlign: "center",
    marginBottom: 10,
  },
  forgotPassword: {
    textAlign: 'right'
  }
});
